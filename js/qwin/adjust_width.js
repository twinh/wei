// TODO getScrollWidth utf8 中文编码
var scroll_width = 18;

function setUiBoxWidth(parent_box, self_box, is_scroll)
{
	/*var self_box = jQuery(self_box);
	var self_box_width = parseInt(jQuery(parent_box).css('width'))
		- parseInt(self_box.css('padding-right'))
		- parseInt(self_box.css('padding-left'))
		- parseInt(self_box.css('border-left-width'))
		- parseInt(self_box.css('border-right-width'))
		- parseInt(self_box.css('margin-left'))
		- parseInt(self_box.css('margin-right'));
	if('undefined' == typeof(is_scroll) || is_scroll == true)
	{
		var self_box_width = self_box_width - scroll_width;
	}
	self_box.css('width', self_box_width);
	return self_box_width;*/
}