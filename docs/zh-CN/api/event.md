[event()](http://twinh.github.com/widget/api/event)
===================================================

Create a new event

##### 目录
* event($type, $namespaces)

### Create a new event
```php
\Event event($type, $namespaces)
```

##### 参数
* **$namespaces** `array` 

